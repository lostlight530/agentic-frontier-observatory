# F4 — Agentic Systems

A2A `referenceTaskIds` remains additional-context linkage rather than typed recovery lineage

MCP Tasks now has an explicit object-state distinction: `SEP-2663` is Final while the Tasks specification remains Draft

Neither fact grants referenced-task authority or successor execution authority
