# F2 — Models / Algorithms / Multimodality

`NO SEPARATE MODEL-MATURITY TRANSITION` is required for today's observatory thesis.

The OpenAI publication reports increased agentic research capability but warns that easily measured coding/experiment metrics are difficult to interpret as direct research progress.

Boundary: `benchmark/internal metric != scientific progress proof`.
