# F1 — History / Theory

`NO MATERIAL CHANGE` in the theoretical framing relevant to authority/recovery.

The four-layer model remains necessary: `SECURITY != EXECUTION != DURABLE EFFECTS != RECOVERY`. Publication maturity of one standards object does not collapse those layers.