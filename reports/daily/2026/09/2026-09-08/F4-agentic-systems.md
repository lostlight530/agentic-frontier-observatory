# F4 — Agentic Systems / Protocols

A2A still uses `referenceTaskIds` as contextual task references and creates a new Task for follow-up/refinement after terminal state. No typed recovery lineage was verified.

MCP `SEP-2663` remains Final while the Tasks extension continues to incubate/evolve. Per-task authentication and authorization checks remain explicit, but no successor-authority inheritance/delegation contract was verified.

`Task reference != Task authority`; `Context Continuity != Authority Continuity`.
