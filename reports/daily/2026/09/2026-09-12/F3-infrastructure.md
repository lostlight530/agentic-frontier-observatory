# F3 — Infrastructure

No portable cross-system contract was verified for committed effects, retained state, rollback/compensation obligations, or recovery completion proof.

`Resource Denial != Runtime Stop`; `Runtime Stop != Rollback`; `Termination != Remediation` remain unchanged.