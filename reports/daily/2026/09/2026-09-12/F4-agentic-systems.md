# F4 — Agentic Systems

A2A current documentation still models refinements through a new Task in the same `contextId` and may reference prior tasks via `referenceTaskIds`. Terminal tasks remain immutable.

No normative typed `predecessor` / `supersedes` / `repairs` / `compensates` recovery lineage or successor-authority contract was verified.

`referenceTaskIds != typed recovery lineage`; `Context Continuity != Authority Continuity`; `Task reference != Task authority`.