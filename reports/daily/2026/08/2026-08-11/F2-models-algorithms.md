# F2｜Models, Algorithms, and Multimodality

No model-level event before the observation cut-off changes the W33 trajectory

Today's finding lives outside raw model capability: even a highly capable model cannot safely use resources it cannot discover, evaluate, authorize, and connect

As native tool/computer use grows, resource selection becomes a separate systems problem rather than a larger-context-window problem
