# F2｜Models, Algorithms, and Multimodality

No same-day model release before the observation cut-off changes the model-level trajectory

The important distinction today is downstream of the model:

- a model or Agent may generate code correctly
- the source may be reviewed
- the artifact may be built with valid provenance
- deployment policy may still reject it

**Rule:** model capability and artifact admissibility remain separate system properties
