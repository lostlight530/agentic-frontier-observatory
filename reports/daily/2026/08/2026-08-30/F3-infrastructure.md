# F3 — Infrastructure / Compute / Data

Infrastructure enforcement remains distributed across:

- identity / policy service
- relying resource
- runtime / session control
- persistent filesystem / state store
- artifact / durable-output systems

Microsoft workload CAE remains strong scoped evidence for resource-side trust re-evaluation, not universal Agent shutdown.

```text
resource denial ≠ runtime stop
credential state ≠ durable-effect state
```
