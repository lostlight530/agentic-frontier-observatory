# Recovery Must Reauthorize / 恢复必须重新授权
## Global AI Daily Research Report / 全球人工智能每日研究报告
### 2026-08-29

**Observation cut-off / 观察截止：2026-08-29 05:37 CST**

## 0｜Executive Judgment / 核心判断

W35 has progressed from live trust to denial, stop, reconciliation and a new recovery-task boundary

Saturday asks the next question:

> **If recovery creates a new execution identity, what authority does that new task actually carry?**

A2A provides unusually direct protocol evidence here

- every incoming request must be authenticated
- the server authorizes requests using the authenticated identity and its own policies
- task operations must remain scoped to the caller's authorization boundary
- in-task authorization can transition a Task into `AUTH_REQUIRED`
- a new Task can preserve `contextId` continuity without reusing the terminal Task identity

This supports a stronger W35 refinement:

> **Recovery re-entry is an authorization boundary**

But the claim must remain precise

> **Fresh authorization check ≠ fresh human approval ≠ fresh credential**

A server may accept an existing valid credential and still perform a new authorization decision for the successor request

## 1｜A2A Evidence / A2A 证据

### Every request is a security boundary

The A2A server authenticates incoming requests according to its declared security requirements and performs authorization based on authenticated identity and implementation-specific policy

Task access operations such as list/get/cancel/subscribe must respect the caller's authorized access boundary

### Context does not carry authority

`contextId` logically groups related Tasks and Messages

It can preserve semantic continuity across a new Task

But authorization is still checked at the request/resource boundary

Therefore:

```text
same context
≠ same task
≠ same authorization result
```

### AUTH_REQUIRED makes authority state explicit inside work

A2A permits an active Task to enter `TASK_STATE_AUTH_REQUIRED` when additional authorization is needed for an action

This shows that authority can change or become insufficient during task execution rather than being treated as a permanent property of the context

## 2｜MCP Evidence / MCP 证据

MCP Tasks remains useful as the contrasting execution model

Its cooperative, eventually consistent cancellation means the old Task may not stop exactly when cancellation is acknowledged

Therefore a recovery controller cannot safely infer the successor Task's authority merely from a cancellation ACK or from the old Task's last visible state

## 3｜Saturday W35 Refinement

The recovery chain is now:

```text
INCIDENT / TRUST CHANGE
→ ORIGINAL TASK
→ CANCEL / STOP
→ TERMINAL + RESIDUAL EVIDENCE
→ REMEDIATION
→ NEW RECOVERY TASK
→ AUTHENTICATE REQUEST
→ AUTHORIZE NEW TASK / ACTION
→ CONTINUE RELATED CONTEXT
→ FOLLOW-UP VALIDATION
```

The strongest Saturday rule is:

> **Context continuity may preserve meaning, but authority must still be evaluated at the new execution boundary**

## 4｜H11 Saturday Status

### H11 — Recovery requires a new execution identity boundary

**STRONGLY STRENGTHENED / REFINED**

Friday established the new Task boundary

Saturday adds explicit request-level authentication/authorization evidence around that boundary

The hypothesis is therefore refined to:

> **Recovery requires a distinct new execution identity whose requests remain independently authorized even when context is preserved**

**Falsifier:** interoperable Agent systems safely recover terminal work by creating successor execution objects whose authorization is implicitly inherited from shared context without new request-level authorization checks

## 5｜Durable Rules

```text
context continuity ≠ authority continuity
new task ≠ inherited authorization
fresh authorization decision ≠ fresh credential
fresh authorization decision ≠ fresh human approval
AUTH_REQUIRED ≠ task failure
authorized task access ≠ authorization for every downstream side effect
```

## 6｜Remaining Recovery Object

```text
incident_id
→ original_task_id
→ terminal_state
→ residual_effect_inventory
→ remediation_actions
→ recovery_task_id
→ recovery_request_identity
→ recovery_authorization_decision
→ delegated-user / sponsor evidence
→ completion evidence
→ follow-up validation
```

This remains an observatory analytical model, not a published cross-protocol schema

## 7｜Daily Workstream Synthesis

- F1: recovery identity and continuity are distinct concepts
- F2: model intelligence does not decide authorization inheritance
- F3: control-plane cancellation and security-plane authorization remain separate
- F4: A2A provides the decisive Saturday evidence
- F5: request-level authorization strengthens governance of successor Tasks
- F6: cross-vendor recovery will require authority semantics in addition to task semantics
- F7: W35 Saturday checkpoint strengthens H11 without manufacturing a new H12

## 8｜Evidence Discipline

```text
A2A authorization ≠ universal Agent authorization standard
authentication ≠ authorization
authorization ≠ user intent
context ≠ principal identity
task ID ≠ credential
new task ≠ incident closure
```

---

> Friday established a new recovery execution boundary  
> Saturday shows that preserving context cannot be allowed to silently preserve authority
