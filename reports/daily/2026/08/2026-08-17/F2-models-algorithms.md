# F2｜Models, Algorithms, and Multimodality

No same-day model event before the observation cut-off changes the broad model trajectory

Today instead reinforces a systems distinction:

```text
MODEL / REASONING CAPABILITY
≠
LIVE EXECUTION AUTHORITY
```

A model may be able to plan and act while the workspace still controls whether that capability is published, who may invoke it, which identity reaches downstream systems, and whether writes require approval

**W34 question:** which governance decisions remain external to the model even as action capability becomes more native
