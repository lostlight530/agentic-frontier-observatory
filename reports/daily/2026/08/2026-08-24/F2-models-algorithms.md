# F2｜Models, Algorithms, and Multimodality

No same-day model release before the cut-off changes the broader W35 question

Model capability can execute long-horizon work, but model reasoning does not itself provide runtime revocation, workload identity rotation, policy enforcement, or incident containment

**Rule:** capability duration and trust duration are different variables
