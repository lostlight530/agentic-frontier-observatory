# F2 — Models, Algorithms, Multimodality

No model release changes today's judgment

The important algorithmic boundary is that a successor Task can inherit semantic context while authorization remains externally evaluated

This prevents model memory/context from becoming an accidental permission cache

**Rule:** remembered context ≠ approved action scope
